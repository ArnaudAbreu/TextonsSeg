from setuptools import setup, find_packages

setup(name='textons',
      version='0.1',
      description='procedures for textons image analysis',
      author='Arnaud Abreu',
      author_email='arnaud.abreu.p@gmail.com',
      packages=find_packages(),
      zip_safe=False,
      install_requires=[],
      include_package_data=True)
